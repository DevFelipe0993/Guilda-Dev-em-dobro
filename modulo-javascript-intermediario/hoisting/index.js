let pais = 'Brasil'
console.log(pais)

// se for declarado depois a variavel ela ira ser lida porem nos bastidores, o JS força uma solicitação de variavel antes de sua chamada.

// Hoisting é o comportamento padrão do JavaScript de mover todas as declarações para o parte superior do escopo atual = içamento